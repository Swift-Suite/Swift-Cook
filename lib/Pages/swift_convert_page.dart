import 'package:flutter/material.dart';

class SwiftConvertPage extends StatelessWidget {
  const SwiftConvertPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.lightBlue);
  }
}
